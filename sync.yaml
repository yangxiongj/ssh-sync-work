# 远程服务器配置
remote:
  host: 34.68.158.244
  port: 22
  dir: /root/work
  os: ubuntu  # 远程操作系统类型: ubuntu, centos, debian, alpine 等

# 同步目录配置
directories:
  # - /mnt/d/PycharmProjects/test

# 刷新间隔 (秒)
refresh_interval: 10

# 排除模式配置
exclude_patterns:
  - .git
  - .idea
  - __pycache__
  # 更多模式...